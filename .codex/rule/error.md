- 2026-05-27: `python3 /Users/jeongjihun/.codex/skills/migrate-to-codex/scripts/migrate-to-codex.py` 실행 시 `ModuleNotFoundError: No module named 'tomllib'` 발생. 원인: 기본 `python3`가 Python 3.11 미만 런타임이라 `tomllib`이 없음. 해결: `/opt/homebrew/bin/python3.12`로 migrator 실행.
- 2026-05-27: migrator 실제 적용 중 `.codex/config.toml` 쓰기에서 `PermissionError: [Errno 1] Operation not permitted` 발생. 원인: OneDrive 작업공간 내 `.codex` 파일 쓰기가 sandbox 권한에 막힘. 해결: 동일 migrator 명령을 승인 권한으로 재실행.
- 2026-05-27: 마이그레이션 산출물 사후 검토를 위해 `mcp__codex_cli__codex` 호출 시 120초 제한으로 타임아웃 발생. 해결: migrator의 `--validate-target`, `--doctor`, `--dry-run` 결과로 검증을 대체하고, 긴 검토가 필요하면 더 작은 범위로 Codex MCP를 호출.
- 2026-05-27: mirror DB cascade 검토 중 `mcp__codex_cli__codex` 교차검토 호출이 120초 제한으로 타임아웃 발생. 해결: 로컬 코드 근거(`sync.py`, `sync_once.py`, `backend/render.yaml`) 중심으로 수동 검토하고, Codex MCP는 ping 수준 확인으로 제한.
- 2026-05-27: `tests/test_sync_once_env.py` 추가 후 첫 실행에서 Render full sync cron env에 `NOTION_DB_SALES`, `NOTION_DB_SEAL_REQUESTS`, `NOTION_DB_SUGGESTIONS` 누락으로 실패. 원인: env 보강 패치가 outbox cron 블록에만 들어가고 full sync 블록에 빠짐. 해결: `dy-task-sync-full` envVars에도 동일 key 추가.
- 2026-05-27: sales update/archive mirror direct + outbox 전환 후 `mcp__codex_cli__codex` 사후 검토 호출이 120초 제한으로 타임아웃 발생. 해결: Codex MCP `ping`으로 연결 확인 후 로컬 pytest 33건과 py_compile 검증 결과를 기준으로 마무리하고, 후속 MCP 검토는 파일/질문 범위를 더 좁혀 호출.
- 2026-05-27: sales link/pdf/quote amount outbox 전환 후 `mcp__codex_cli__codex` 사후 개념 검토 호출이 실패. 원인: OneDrive 한글 경로가 `x-codex-turn-metadata` websocket header UTF-8 변환 오류를 유발했고, 재시도 후 `reasoning.effort='minimal'`과 web_search tool 조합 invalid_request 발생. 해결: Codex MCP `ping` 또는 `reasoningEffort='low'` 이상으로 좁은 범위 호출, 필요 시 ASCII 경로 worktree에서 MCP 검토.
- 2026-05-27: 리뷰 후 `get_sync().upsert_page(..., skip_active_outbox=False)` 보강 중 `backend/app/routers/sales/__init__.py`의 nested try 블록 들여쓰기 누락으로 pytest collection/py_compile에서 `IndentationError` 발생. 해결: nested try 내부 호출을 2칸 들여쓰기 규칙에 맞게 정렬하고 py_compile 재실행.
- 2026-05-27: outbox/date 보강 검증 중 `pytest tests/test_contracts.py` 및 단일 테스트 `test_create_end_before_start_400`가 출력 없이 장시간 멈춤. 원인 미확정(해당 파일의 TestClient/app 또는 DB fixture 초기화 가능성). 해결: 멈춘 pytest 프로세스를 종료하고, 직접 변경 범위는 `test_notion_outbox.py`, `test_project_date_props.py`, `test_projects_mirror_write.py`, `py_compile`로 검증. 추후 contracts 테스트는 별도 fixture 초기화 경로를 분리 점검.
- 2026-05-28: `tests/test_health_routes.py` 첫 실행에서 로컬 `.env`의 `WORKS_BOT_ENABLED=true` 때문에 startup 검증이 `WORKS_BOT_ID`, `WORKS_BOT_SERVICE_ACCOUNT_ID`, `WORKS_BOT_PRIVATE_KEY` 누락으로 실패. 원인: health route 테스트가 외부 서비스 설정에 불필요하게 의존. 해결: 테스트 fixture에서 `WORKS_ENABLED`, `WORKS_BOT_ENABLED`, `WORKS_DRIVE_ENABLED`를 false로 고정.
- 2026-05-28: 견적 VAT/문서번호 보강 검증 중 `cd frontend && npx tsc --noEmit`이 `.next/types/routes.d-Jeong의 MacBook Air.ts`의 `Duplicate identifier 'LayoutProps'`로 실패. 원인: Next.js 생성 타입 파일이 로컬 장치명 포함 중복 산출물로 남아 있음. 해결: 이번 변경 검증은 backend pytest/py_compile과 변경 TS 파일의 정적 확인으로 대체하고, frontend 전체 typecheck 전에는 `.next` 생성물을 정리 후 재실행.
- 2026-05-28: `cd backend && pytest tests/ -x` 첫 실행에서 로컬 `.env`의 `WORKS_BOT_ENABLED=true` 때문에 startup 검증이 `WORKS_BOT_ID`, `WORKS_BOT_SERVICE_ACCOUNT_ID`, `WORKS_BOT_PRIVATE_KEY`, `WORKS_CLIENT_ID`, `WORKS_CLIENT_SECRET` 누락으로 실패. 원인: 전체 테스트가 로컬 외부 서비스 env를 그대로 상속. 해결: 전체 테스트 재실행 시 `WORKS_BOT_ENABLED=false WORKS_ENABLED=false WORKS_DRIVE_ENABLED=false`를 명시해 외부 연동을 끄고 실행.
- 2026-05-28: cashflow create mirror-first 보강 후 `ruff check ... --select F,I,UP,E` 첫 실행에서 `app/routers/cashflow.py` import 정렬 오류(`I001`) 발생. 원인: 신규 outbox 상수 import 추가 후 import block 미정렬. 해결: 동일 ruff 명령에 `--fix`를 붙여 자동 정렬 후 재검증.
- 2026-05-29: `/me` 일정 탭 hang 보강 검증 중 `cd frontend && npx tsc --noEmit`이 `.next/types/routes.d-Jeong의 MacBook Air.ts`의 `Duplicate identifier 'LayoutProps'`로 실패. 원인: Next.js 생성 타입 캐시에 장치명 suffix 파일과 기본 `routes.d.ts`가 함께 남아 중복 선언. 해결: frontend `.next` 생성물을 정리한 뒤 typecheck를 재실행.
- 2026-05-30: Supabase 상태 점검 중 `create_engine(DATABASE_URL)` 실행 시 `ModuleNotFoundError: No module named 'psycopg2'` 발생. 원인: 로컬 venv에 SQLAlchemy 기본 postgres 드라이버인 psycopg2가 설치되지 않음. 해결: 이미 설치된 `psycopg` 드라이버가 있으면 URL driver를 `postgresql+psycopg://`로 바꿔 읽기 전용 진단을 재시도.
- 2026-05-30: ESLint 9 flat config 환경에서 `npm run lint -- --file app/me/page.tsx ...` 실행 시 `Invalid option '--file'` 발생. 원인: flat config에서는 Next.js 구버전 CLI의 `--file` 옵션을 사용할 수 없음. 해결: `npm run lint -- app/me/page.tsx components/...`처럼 대상 파일 경로를 직접 인자로 전달.
- 2026-05-30: `/me` 담당 프로젝트/내 영업에서 `모두 접기` 후 `TASK 있는 항목 펼치기`가 작동하지 않음. 원인: 펼침/접힘을 별도 counter prop으로 두어 접기 counter가 바뀐 뒤 계속 우선 판정됨. 해결: `type + version` 단일 collapse command로 마지막 버튼 명령만 해석하도록 변경.
- 2026-05-30: `/me` 프로젝트 로딩이 Render 로그에 응답 완료 없이 hang처럼 보임. 원인 후보: `ALTER TABLE mirror_projects ADD COLUMN contract_folder_id`가 `ACCESS EXCLUSIVE` lock 대기 상태로 길게 걸리면 이후 `mirror_projects` SELECT도 lock queue 뒤에서 대기할 수 있음. 완화: `/me` 담당 프로젝트는 `completed=false`로 조회하고, 프로젝트 가져오기 검색은 전체 목록 fetch 대신 서버 검색 `q + limit`로 제한. 재발 시 `pg_stat_activity`/`pg_blocking_pids`로 blocker 확인 후 migration은 트래픽 낮은 시간대에 실행.
- 2026-05-30: `/me` 프로젝트 hang 재발 시 `pg_stat_activity`에서 `SELECT ... FROM mirror_projects ...`가 `idle in transaction`으로 2분 이상 남음. 원인: FastAPI yield dependency cleanup이 응답 전송 이후라 `/api/projects` 응답이 느리거나 클라이언트가 끊기면 read transaction이 pool connection을 계속 점유. 해결: `list_projects`에서 DTO 조립 직후 `db.rollback()`으로 read transaction을 먼저 종료.
- 2026-05-30: `cd backend && source .venv/bin/activate && pytest tests/ -x` 실행 시 로컬 `.env`의 `WORKS_BOT_ENABLED=true` 때문에 startup 검증이 `WORKS_BOT_ID`, `WORKS_BOT_SERVICE_ACCOUNT_ID`, `WORKS_BOT_PRIVATE_KEY`, `WORKS_CLIENT_ID`, `WORKS_CLIENT_SECRET` 누락으로 실패. 해결: 전체 테스트 재실행 시 `WORKS_BOT_ENABLED=false WORKS_ENABLED=false WORKS_DRIVE_ENABLED=false`를 명시해 외부 연동을 끄고 실행.
- 2026-05-30: 주간업무일지 원인 분석 중 `rg ... backend/app/services/weekly_report.py`가 `No such file or directory`로 실패. 원인: `weekly_report`가 단일 파일에서 `backend/app/services/weekly_report/` 패키지로 분리되어 있음. 해결: 먼저 `rg --files backend/app/services/weekly_report*` 또는 패키지 디렉터리 기준으로 검색.
- 2026-05-31: 비용 절감 패치 검증 중 `cd backend && source .venv/bin/activate && pytest tests/ -x`가 로컬 `.env`의 `WORKS_BOT_ENABLED=true` 때문에 startup 검증에서 `WORKS_BOT_ID`, `WORKS_BOT_SERVICE_ACCOUNT_ID`, `WORKS_BOT_PRIVATE_KEY`, `WORKS_CLIENT_ID`, `WORKS_CLIENT_SECRET` 누락으로 실패. 해결: 전체 테스트 재실행 시 `WORKS_BOT_ENABLED=false`를 명시하고, 필요하면 `WORKS_ENABLED=false WORKS_DRIVE_ENABLED=false`도 함께 둔다.
- 2026-05-31: `WORKS_BOT_ENABLED=false`로 전체 pytest를 재실행했지만 `tests/test_auth_password_regression.py`의 TestClient startup/teardown 구간에서 출력 없이 장시간 멈춤. 원인 후보: lifespan의 외부 schema 보강 background task가 로컬 `.env`의 Notion 설정을 잡고 네트워크 대기. 해결: 변경 범위 검증은 `py_compile`, YAML 파싱, outbox/sync 관련 순수 단위 테스트로 좁히고, TestClient 검증 시에는 외부 연동 env를 완전히 비우거나 mock한다.
- 2026-05-31: `get_current_user(_bearer(token), db)` 직접 호출 테스트에서 cookie shadow 검증이 FastAPI `Cookie(default=None)` 객체를 JWT 문자열로 decode하려 해 `AttributeError: 'Cookie' object has no attribute 'rsplit'` 발생. 원인: FastAPI DI 밖에서 기본값은 실제 `None`이 아니라 parameter marker 객체. 해결: `dy_jwt`가 `str`일 때만 cookie token으로 취급하고, 그 외에는 `None`으로 정규화한다.
- 2026-05-31: 날인 2차 승인 후 자동 생성된 `[날인 2차검토]` TASK가 `진행 중`/실제 완료일 공란으로 남음. 원인: 자동 TASK 생성 background가 노션 날인요청 row의 `2차검토TASK` rich_text만 갱신하고 local `mirror_seal_requests.properties`에는 즉시 반영하지 않아, 5분 sync 전 최종 승인 시 승인 라우터가 TASK page_id를 못 읽고 완료 동기화를 스킵할 수 있음. 해결: TASK 링크 생성 직후 mirror에도 링크를 write-through 병합하고, 승인/반려 완료 동기화 background에서 mirror id가 비어 있으면 노션 원본 row를 조회해 TASK id를 보강한다.
- 2026-05-31: 변경 범위 확인 중 `ruff check app/services/seal_logic.py app/routers/seal_requests/__init__.py ...`가 기존 `seal_requests/__init__.py`의 미사용 import/B008/N812 경고까지 함께 잡아 실패. 원인: 파일 전체 lint와 이번 변경 범위가 맞지 않음. 해결: 신규 수정으로 발생한 import 정렬은 `ruff check app/routers/seal_requests/list_endpoint.py --select I --fix`로 고치고, 기존 경고는 별도 정리 작업으로 분리한다.
- 2026-05-31: 담당팀 sync 로직 조사 중 `rg ... backend .github .claude render.yaml Procfile Dockerfile docker-compose.yml`가 루트에 없는 `render.yaml`/`Procfile`/`Dockerfile`/`docker-compose.yml` 인자로 인해 exit 2를 반환. 원인: 실제 Render blueprint는 `backend/render.yaml`에 있음. 해결: 선택 파일 경로는 `test -f`로 확인하거나 `rg` 대상은 존재하는 디렉터리/파일만 넘긴다.
- 2026-05-31: 담당팀 sync 보강 후 `source .venv/bin/activate && pytest tests/ -x`가 로컬 `.env`의 `WORKS_BOT_ENABLED=true` 때문에 startup 검증에서 Bot 자격 누락으로 실패했고, `WORKS_BOT_ENABLED=false pytest tests/ -x` 재시도는 `tests/test_auth_password_regression.py` TestClient 구간에서 장시간 출력 없이 멈춤. 해결: 변경 범위는 순수 sync 단위 테스트로 검증하고, 전체 TestClient 검증은 `WORKS_BOT_ENABLED=false WORKS_ENABLED=false WORKS_DRIVE_ENABLED=false` 등 외부 연동 env를 완전히 끈 별도 세션에서 실행한다.
- 2026-05-31: 담당팀 sync 보강 후 `ruff check app/services/sync.py tests/test_sync_assignee_team_policy.py`가 기존 `sync.py`의 `mirror as M`, `notion_props as P`, `seal_logic as SL` 별칭 규칙(N812)으로 실패. 원인: 레거시 파일 전체 lint와 변경 범위 불일치. 해결: 이번 변경 검증은 `ruff check ... --select F,E,I,UP`로 좁히고, N812 별칭 정리는 별도 작업으로 분리한다.
- 2026-05-31: 견적서 자동 TASK 제목 수정 중 `rg -n "견적서 작성" backend/app tests -S`가 루트에 없는 `tests` 경로 때문에 exit 2를 반환. 원인: 검색 대상 경로 존재 여부를 확인하지 않고 넘김. 해결: `rg --files` 또는 `test -d tests`로 경로 존재를 먼저 확인한 뒤 검색한다.
- 2026-05-31: 견적서 자동 TASK 제목 수정 후 `ruff check app/routers/sales/__init__.py tests/test_sales_mirror_write.py --select F,E,I,UP`가 기존 import 정렬(I001), 미사용 import(F401), `timezone.utc` 권고(UP017)를 잡아 실패. 원인: 변경 범위보다 넓은 파일 단위 lint가 레거시 이슈까지 포함. 해결: 이번 검증은 `py_compile`과 대상 테스트로 제한하고, 해당 lint 정리는 별도 작업으로 분리한다.
- 2026-06-01: 견적서 저장 후 프론트가 `저장 중…`에서 장시간 대기. 원인: 견적 추가 뒤 자동 생성하는 "견적서 작성" TASK가 Notion `create_page` 기본 background client(최대 60초 retry)를 사용해 사용자 응답 path를 붙잡음. 해결: 자동 TASK 생성도 `user_facing=True`로 호출해 5초 deadline + 4초 SDK timeout 정책을 적용하고, 실패는 기존처럼 warning으로 흡수한다.
- 2026-06-01: 견적 저장 hang 보강 중 `backend/app/routers/sales/__init__.py`에 주석/호출부 들여쓰기를 한 단계 깊게 넣어 `IndentationError: unexpected indent` 발생. 해결: `try` 블록 내부의 `props`와 같은 레벨로 정렬하고 `python -m py_compile app/routers/sales/__init__.py`로 즉시 확인.
- 2026-06-01: Render 로그에 `/api/health/db` 200 응답마다 `long DB connection held 350ms`가 반복 출력됨. 원인: Render 헬스체크가 5초 간격으로 DB health를 호출하고, 기존 health query가 `SELECT 1` + `SHOW idle_in_transaction_session_timeout` 두 번의 DB round-trip을 수행했으며 DB checkout 경고 임계값이 0.3초로 낮아 평시 Singapore↔Supabase 지연도 경고로 잡힘. 해결: health query를 `current_setting('idle_in_transaction_session_timeout')` 단일 조회로 줄이고 checkout 경고 임계값을 SQL slow 기준과 같은 0.5초로 조정한다.
- 2026-06-02: 대시보드 수금/지출 fetch를 `ChartsTabs` 내부 lazy load로 옮긴 뒤 `frontend/app/page.tsx` 렌더 조건에 기존 `incomes` 참조가 남아 `Cannot find name 'incomes'` 타입 오류 발생. 해결: 부모의 렌더 조건/props에서 이동된 데이터 참조를 모두 제거하고, 데이터 이동 시 호출부 조건까지 함께 검색한다.
- 2026-06-04: `cd frontend && npm run dev -- -p 3001` 실행 후 `/operations/incomes` 확인 중 Turbopack 내부 panic(`range start index ... out of range for slice ...`, `turbo-persistence/src/static_sorted_file.rs`)으로 dev 서버가 종료됨. 원인 후보: `.next/cache` 또는 Turbopack persistence 캐시 손상. 해결: 재발 시 frontend `.next` 생성물을 정리한 뒤 dev 서버를 재실행하고, 전체 typecheck는 `.next/types` 중복 산출물 정리 후 실행한다.
- 2026-06-08: 영업 `영26-206` 묶음 PDF 다운로드 시 외부 견적 첨부 `외부_영26206_풍동실험.pdf`에서 `외부 견적 첨부 PDF 다운로드 실패` 발생. 원인: `get_download_url()`로 받은 NAVER WORKS Storage URL은 발급되지만, 실제 파일 GET 시 `Authorization: Bearer ...` 헤더 없이 호출해 Storage가 `401 UNAUTHORIZED Authentication failed`를 반환함. 해결: signed URL 실제 다운로드 단계에도 유효한 WORKS Drive access token을 Bearer 헤더로 전달하거나, 다운로드 URL이 인증 불필요인지 API 동작 변경 여부를 재확인한다.

- 2026-06-16: 주간업무일지 퇴사자 task 조사 중 `rg -n "create_engine|..." backend\tests ... backend` 및 `git diff -- ...`가 14초 제한으로 timeout. 원인: 검색/diff 범위가 현재 작업에 비해 넓고 OneDrive 작업공간 I/O가 느림. 해결: `rg --files`로 대상 파일을 먼저 좁히고, 필요한 파일만 `Get-Content`/개별 검증으로 확인한다.
- 2026-06-16: 주간업무일지 테스트 중 `backend\.venv\Scripts\python.exe`가 존재하지 않아 CommandNotFound, 이후 `backend\.venv\bin\python -m pytest tests\test_weekly_report_team.py -q`가 120초 timeout. 원인: 이 작업공간 venv는 Windows Scripts 구조가 아니라 bin shim 구조이며, pytest는 앱/fixture import 단계에서 출력 없이 지연될 수 있음. 해결: venv 구조를 먼저 확인하고, 필요 시 변경 범위 helper import/개별 테스트부터 좁게 검증한다.
- 2026-06-16: macOS 경로(`/opt/homebrew/...`)를 가리키는 `backend\.venv` 때문에 Windows PowerShell에서 `.venv\bin\python --version`과 파일 읽기가 timeout. `uv run python --version`은 성공했지만 `app.services.weekly_report.helpers` import는 60초 timeout. 원인: OneDrive에 복사된 venv shim이 현재 OS와 맞지 않고, 앱 import가 로컬 설정/모델 초기화에 오래 걸릴 수 있음. 해결: Windows에서는 `uv run ...` 또는 OS별 venv 재생성 후, import-heavy pytest 전에 `py_compile`/좁은 단위 검증부터 수행한다.
- 2026-06-16: 주간업무일지 퇴사자 필터 변경 후 `git diff --stat`, 대상 파일 `git diff -- ...`, `uv run pytest tests\test_weekly_report_team.py -q`가 각각 timeout. 원인: OneDrive 작업공간에서 git diff가 느리고, pytest는 앱 import/fixture 단계가 장시간 지연됨. 해결: 대상 변경 지점은 `Select-String`으로 확인하고, 문법은 `uv run python -m py_compile ...`로 검증한다.